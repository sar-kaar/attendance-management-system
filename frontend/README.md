# Frontend

React + Vite + MUI frontend for the Attendance Management System.

## Setup

```bash
npm install
npm run dev
```

## Environment Variables

Copy `.env.example` to `.env` and configure:

```
VITE_API_URL=https://your-backend.azurewebsites.net/api
```
