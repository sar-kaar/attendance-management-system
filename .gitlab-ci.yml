stages:
  - test
  - build
  - deploy

variables:
  AZURE_RESOURCE_GROUP: ams-rg
  AZURE_WEBAPP_NAME: ams-backend
  AZURE_STORAGE_ACCOUNT: amsfrontendweb

test:
  stage: test
  image: python:3.11
  variables:
    SECRET_KEY: ci-test-secret-key-not-for-production
    DEBUG: "True"
    ALLOWED_HOSTS: localhost,127.0.0.1
    CORS_ALLOWED_ORIGINS: http://localhost:3000,http://127.0.0.1:3000
    PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  cache:
    key:
      files:
        - backend/requirements.txt
    paths:
      - .cache/pip
  before_script:
    - cd backend
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - pip install --no-deps face-recognition==1.3.0
  script:
    - python manage.py makemigrations --check
    - python manage.py check
    - python manage.py test --verbosity=2
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "develop"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'

build-frontend:
  stage: build
  image: node:20
  variables:
    VITE_API_URL: "https://ams-backend.azurewebsites.net/api"
  script:
    - cd frontend
    - npm ci
    - npm run build
  artifacts:
    paths:
      - frontend/dist
    expire_in: 1 hour
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  needs:
    - test

deploy-backend:
  stage: deploy
  image: mcr.microsoft.com/azure-cli:latest
  script:
    - az login --service-principal --username "$AZURE_CLIENT_ID" --password="$AZURE_CLIENT_SECRET" --tenant "$AZURE_TENANT_ID"
    - az account set --subscription "$AZURE_SUBSCRIPTION_ID"
    - python3 -c "import shutil; shutil.make_archive('backend', 'zip', 'backend')"
    # --async true: upload the zip, then let the CLI track build/start via short
    # ARM status polls. A plain synchronous deploy holds one HTTP request open
    # while Oryx rebuilds numpy/dlib-bin (often >230s on B1), and Azure's front
    # door cuts that request off - false-failing the job even though the deploy
    # succeeds. The CLI's own tracking exits 0/1 based on the real final status.
    - az webapp deploy --resource-group "$AZURE_RESOURCE_GROUP" --name "$AZURE_WEBAPP_NAME" --src-path backend.zip --type zip --async true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  needs:
    - test

deploy-frontend:
  stage: deploy
  image: mcr.microsoft.com/azure-cli:latest
  script:
    - az login --service-principal --username "$AZURE_CLIENT_ID" --password="$AZURE_CLIENT_SECRET" --tenant "$AZURE_TENANT_ID"
    - az account set --subscription "$AZURE_SUBSCRIPTION_ID"
    - az storage blob upload-batch --account-name "$AZURE_STORAGE_ACCOUNT" --destination '$web' --source frontend/dist --auth-mode login --overwrite
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  needs:
    - build-frontend
